services:
  server:
    build: ./server
    container_name: nest-server
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env

  client:
    build: ./client
    container_name: react-client
    ports:
      - "5173:80"
    depends_on:
      - server